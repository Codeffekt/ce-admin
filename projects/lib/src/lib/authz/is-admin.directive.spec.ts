import { IsAdminDirective } from "./is-admin.directive";

describe('IsSuperAdminDirective', () => {
  it('should create an instance', () => {
    // const directive = new IsAdminDirective();
    // expect(directive).toBeTruthy();
  });
});
