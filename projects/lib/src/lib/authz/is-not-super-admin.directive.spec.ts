import { IsNotSuperAdminDirective } from './is-not-super-admin.directive';

describe('IsNotSuperAdminDirective', () => {
  it('should create an instance', () => {
    // const directive = new IsNotSuperAdminDirective();
    // expect(directive).toBeTruthy();
  });
});
