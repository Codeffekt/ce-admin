import { HasAllResourceDirective } from './has-all-resource.directive';

describe('HasAllResourceDirective', () => {
  it('should create an instance', () => {
    /* const directive = new HasAllResourceDirective();
    expect(directive).toBeTruthy(); */
  });
});
